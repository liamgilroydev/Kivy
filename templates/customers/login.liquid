<!-- /templates/customers/login.liquid -->

<div class="container py-5 my-5">
    <div class="login-form">
        {% form 'customer_login' %}
            {% include 'form-error', form: form %}
            <h3 class="h3">Customer Login</h3>
            <label for="emailAddress">Email Address</label>
            <input type="email" name="customer[email]" class="form-control rounded-0 my-2">
            <label for="password">Password</label>
            <input type="password" name="customer[password]" class="form-control rounded-0 my-2">

            <input type="submit" value="Sign in" class="btn btn-primary btn-lg w-100 rounded-0">
        {% endform %}

        <a href="#recover" id="forgotPassword" class="text-center">Forgot your password?</a>
    </div>
    <div class="d-none forgot-password-form my-5" id="forgot_password_form">
        {% form 'recover_customer_password' %}
            {% include 'form-error', form: form %}

            <div class="col-12">
                <label for="resetPassword" class="form-label">Email Address</label>
                <input class="form-control" type="email" name="email" id="resetPassword">
            </div>

            <input class="btn btn-secondary" type="submit" value="Reset Password">

        {% endform %}
    </div>
</div>



